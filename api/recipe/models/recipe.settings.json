{
  "kind": "collectionType",
  "collectionName": "recipes",
  "info": {
    "name": "Recipe",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "ingredient": {
      "type": "component",
      "repeatable": true,
      "component": "recipes.ingredient"
    },
    "steps": {
      "type": "component",
      "repeatable": true,
      "component": "recipes.steps"
    },
    "images": {
      "collection": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": false
    },
    "tags": {
      "collection": "tags",
      "via": "recipes",
      "dominant": true
    }
  }
}
